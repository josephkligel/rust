// Equivalent to
// use std::cmp::Ordering;
// use std::io;
//use std::{cmp::Ordering, io};

// Equivalent to
// use std::io;
// use std::io::Write;
use std::io::{self, Write};

fn main() {
    println!("Hello, world!");
}
